import { Sudoku } from "../models/sudoku.js";

export class SudokuUtils {
    static movValido(sudoku: Sudoku, row: number, col: number, num: number):boolean {
        const tabuleiro = sudoku;
        //Verificar Linha do tabuleiro
        return false
    }
}